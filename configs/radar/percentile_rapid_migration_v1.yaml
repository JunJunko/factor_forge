version: 1
id: percentile_rapid_migration_v1
kind: composite_anomaly
description: 标准化真实波幅的历史分位在短窗口内快速迁移
observation_type: relation_anomaly
data: {required_fields: [adj_close, adj_high, adj_low], universe_field: is_liquid, entity_field: ts_code, date_field: trade_date, industry_field: industry_l1_code}
scan: {discovery_window_days: 252, recent_window_days: 20}
filters: {min_listing_days: 250, exclude_st: true, exclude_suspended: true, exclude_limit_locked: true}
quality_gate: {min_events: 150, min_unique_stocks: 60, min_unique_industries: 10, max_industry_share: 0.40, max_single_stock_share: 0.05}
parameters: {recipe: percentile_rapid_migration, history: {window: 252, min_periods: 120}, short_window: 5, long_window: 14, upper_percentile: 0.90, lower_percentile: 0.10, change_threshold: 0.50, min_cross_section: 20}
